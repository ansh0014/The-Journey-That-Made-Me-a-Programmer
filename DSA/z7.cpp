// now i am doing the remove duplicates from sorted array 11
// i have given an integer array nums sortd in no-decresing order, remove some duplciaes in place such that each unique element appears at most twice. the rlative order of the elements elemtnes shold be kept the same.
// since it is impossible to change the length of the array in 
// we used the sliding window technique to remove the duplicates from the sorted array
#include <bits/stdc++.h>
using namespace std;
class Solution{
    public:
       int removeDuplicates(vector<int>& nums) {
    int n=nums.size();
    if(n==0) return 0;

    }
};